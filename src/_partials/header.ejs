<header class="c-header">  
  <div class="c-header__main">
    <div class="c-header__left">
      <a class="c-header__logo" href="<%= BASE_URL %>index.html"><img src="./assets/img/logo.png" alt="Matsuguchi"></a>
    <p class="c-header__slogan">松口内科・循環器内科クリニック</p>
    </div>
    <nav class="c-header__nav">
      <ul class="c-header__menu">
        <% if (typeof linklists != 'undefined') { %>
          <% linklists.main && linklists.main.forEach(link => { %>
            <li class="c-header__item <%= link.class %>">
              <img class="c-header__icon" src="<%= link.icon %>" alt="<%= link.title %>">
              <a class="c-header__link <% if (link.current || link.active) { %>is-active<% } %>" href="<%= link.url %>"><%= link.title %></a>
            </li>
          <% }) %>
        <% } %>
      </ul>
    </nav>
    <div class="c-header__right">
      <a href="#" class="c-header__btn"><span class="c-header__txt">WEB 予約</span></a>
      <p class="c-header__tel">092-407-5555</p>
    </div>
  </div>
</header>
